export class Warehouse {
  constructor(
    public id: string = '',
    public name?: string,
    public capacity?: number,
    public location?: string,
    public mas?: number,
    public status?: boolean,
  ) {
    this.id = '';
  }
}

