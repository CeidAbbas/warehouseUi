import { PackageWarehouseInventory } from './package-warehouse-inventory';

describe('PackageWarehouseInventory', () => {
  it('should create an instance', () => {
    expect(new PackageWarehouseInventory()).toBeTruthy();
  });
});
