import { WarehouseInventory } from './warehouse-inventory';

describe('WarehouseInventory.Model', () => {
  it('should create an instance', () => {
    expect(new WarehouseInventory()).toBeTruthy();
  });
});
