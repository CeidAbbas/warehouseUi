import { WarehouseResponsible } from './warehouse-responsible';

describe('WarehouseResponsible', () => {
  it('should create an instance', () => {
    expect(new WarehouseResponsible()).toBeTruthy();
  });
});
